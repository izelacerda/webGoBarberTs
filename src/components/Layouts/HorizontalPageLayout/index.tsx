import React from 'react';

import { Container } from './styles';

const HorizontalPageLayout: React.FC = ({ children }) => {
  return <Container>{children}</Container>;
};

export default HorizontalPageLayout;
